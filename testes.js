function squareSum(numbers) {
    let sum;
    const result = numbers.reduce((acc, val) => {
        sum 
    }, 0);
    
}

console.log(squareSum([1, 2]));
// Faça a raiz quadrada dos números do array, some os resultados e exiba na tela.

// 1. Quais são os dados de entrada necessários?
    // Um array de números
// 2. O que devo fazer com estes dados?
    // Fazer a raiz quadrada, somar os resultados e exibir.
// 3. Quais são as restrições deste problema?
    // Somente números são aceitos no array
// 4. Qual é o resultado esperado?
    // Mostrar o resultado da soma das raizes quadradas dos números.
// 5. Qual é a sequência de passos a serem feitas para chegar ao resultado esperado?
    // Pegar o array.
    // Realizar a potência elevada a dois dos números.
    // Somar + 1 ao resultado da raiz.
    // Retornar o resultado.
    // 